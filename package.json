{
  "name": "skill-icons-updated",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "build": "node build.ts",
    "deploy-worker": "wrangler deploy",
    "deploy": "node build.ts && wrangler publish"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "prettier": "^2.8.8",
    "wrangler": "^3.114.0"
  }
}
